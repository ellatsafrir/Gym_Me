<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@android:color/white"
    android:layout_gravity="center">


    <!-- Edit Workout Name -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Edit Workout Name"
        android:textSize="16sp"
        android:textColor="#000000"
        android:layout_marginBottom="8dp"/>

    <EditText
        android:id="@+id/editWorkoutNameEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter new workout name"
        android:inputType="text"
        android:textSize="14sp"
        android:textColor="#000000"
        android:layout_marginBottom="16dp"
        android:background="@android:drawable/edit_text"/>

    <!-- Edit Sets Section -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Edit Sets"
        android:textSize="16sp"
        android:textColor="#000000"
        android:layout_marginBottom="8dp"/>

    <!-- Dynamically populated sets go here -->
    <LinearLayout
        android:id="@+id/editSetsContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_marginBottom="16dp">
        <!-- Dynamically populated set items go here -->
    </LinearLayout>

    <!-- Add New Set Button -->
    <Button
        android:id="@+id/addNewSetButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Add New Set"
        android:textSize="16sp"
        android:layout_marginTop="8dp"
        android:background="@android:color/holo_green_light"
        android:textColor="#FFFFFF"/>

    <!-- Save Changes Button -->
    <Button
        android:id="@+id/saveWorkoutChangesButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Save Changes"
        android:textSize="16sp"
        android:layout_marginTop="8dp"
        android:background="@android:color/holo_blue_light"
        android:textColor="#FFFFFF"/>

    <!-- Delete Workout Button -->
    <Button
        android:id="@+id/deleteWorkoutButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Delete Workout"
        android:textSize="16sp"
        android:layout_marginTop="8dp"
        android:background="@android:color/holo_red_light"
        android:textColor="#FFFFFF"/>

    <!-- Cancel Button -->
    <Button
        android:id="@+id/cancelWorkoutEditButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Cancel"
        android:textSize="16sp"
        android:layout_marginTop="8dp"
        android:background="@android:color/darker_gray"
        android:textColor="#FFFFFF"
        android:layout_marginBottom="16dp"/>
</LinearLayout>
